# rn-performace-test-flipper

clone this repo 
run following commands

```bash
yarn install
cd ios && pod install
cd ..
yarn ios
```

Search add `rn-perf-monitor` in the list of plugins of flipper.

Doesn't have android setup.
